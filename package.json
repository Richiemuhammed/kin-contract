{
  "name": "@kin/contract",
  "version": "1.0.0",
  "description": "Shared API contract between Kin backend and frontend",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "prepare": "npm run build",
    "generate:openapi": "tsx src/openapi.ts"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.9.2",
    "@asteasolutions/zod-to-openapi": "^7.3.4"
  },
  "private": true
}
